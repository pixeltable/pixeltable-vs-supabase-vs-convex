# The only variable the Function reads. `withSupabase` injects the database client, so
# no URL or key belongs here. `localhost` is the Edge Runtime container itself; the
# compute service runs on your machine.
COMPUTE_SERVICE_URL=http://host.docker.internal:9000
