[project]
name = "platform-comparison-modal"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "modal",
    "openai",
    "supabase",
]

[project.optional-dependencies]
test = ["pytest", "httpx"]
