# Project-specific
.env*
!.env.example
node_modules/
.DS_Store
*.mp4
*.mov
*.mkv
*.webm
*.avi
*.wav
*.mp3
package-lock.json
uv.lock
deno.lock
# Needs an authenticated Convex deployment to generate; see convex-app/README.md.
convex-app/convex/_generated/
supabase-app/supabase/.branches/
supabase-app/supabase/.temp/
# docs/metrics.json is committed on purpose: every number in the docs comes from it,
# and CI fails if a code change leaves it stale.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editors
.idea/
.vscode/
